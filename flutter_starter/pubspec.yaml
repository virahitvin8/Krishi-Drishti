name: krishidrishti
description: Krishi Drishti - Satellite Vision for Smart Farming (Flutter App)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # Add these later when needed:
  # http: ^1.2.2
  # flutter_map: ^7.0.0
  # webview_flutter: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
