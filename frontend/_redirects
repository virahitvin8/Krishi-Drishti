# Netlify Redirects - Krishi Drishti
# SPA fallback: serve index.html for all routes
/*    /index.html    200

# API calls to backend (optional - can use direct URL instead)
# /api/*  https://krishi-drishti-backend.onrender.com/api/:splat  200
